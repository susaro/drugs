\ProvidesPackage{preamble}

%\usepackage{geometry}
%\geometry{a4paper, top=25mm, left=0mm, right=0mm, bottom=25mm}

\usepackage{amsmath,amsthm}
\usepackage{amssymb}
\usepackage{graphicx}
%\usepackage{lineno}
\usepackage{textcomp}
%\usepackage{parskip}
\usepackage{subfigure}
%\usepackage{color}
\usepackage[refeq,refpage]{nomencl}
\usepackage{listings}
\usepackage[sort&compress,numbers,comma,merge]{natbib}
\usepackage{bbold}
\usepackage{tikz-cd}
\usepackage{mathtools}
\usepackage{tikz}
\usetikzlibrary{arrows,matrix,positioning}

%for tables
\usepackage{booktabs}

%font
%\usepackage{mathpazo} % add possibly `sc` and `osf` options
%\usepackage{eulervm}
%\usepackage{palatino,eulervm}

\newcommand{\mailto}[1]{\href{mailto:#1}{#1}}

%-----------------------------------------------------------------------
%Theorems
\newtheoremstyle{normal}% normale Schrift
{10pt}% hSpace abovei
{10pt}% hSpace belowi
{\normalfont}% hBody fonti
{}% hIndent amounti1
{\bfseries}% hTheorem head fonti
{}% Punctuation after theorem headi
{0.8em}% hSpace after theorem headi2
{\bfseries{\thmname{#1}\thmnumber{ #2}.\thmnote{ \hspace{0.5em}(#3)\newline}}}% hTheorem head spec (can be left empty, meaning `normal')
 
\newtheoremstyle{kursiv}% kursive Schrift
{10pt}% hSpace abovei
{10pt}% hSpace belowi
{\itshape}% hBody fonti
{}% hIndent amounti1
{\bfseries}% hTheorem head fonti
{}% Punctuation after theorem headi
{0.8em}% hSpace after theorem headi2
%{}% hTheorem head spec (can be left empty, meaning `normal')
{\bfseries{\thmname{#1}\thmnumber{ #2}.\thmnote{ \hspace{0.5em}(#3)\newline}}}% hTheorem head spec (can be left empty, meaning `normal')

\theoremstyle{normal}
\newtheorem{chap}{Proposition}[chapter]
\newtheorem{defi}[chap]{Definition}
\newtheorem{ex}[chap]{Example}
\newtheorem{rem}[chap]{Remark}
\newtheorem{cor}[chap]{Corollary}
\newtheorem{erin}[chap]{Erinnerung}

\theoremstyle{kursiv}
\newtheorem{lem}[chap]{Lemma}
\newtheorem{thm}[chap]{Theorem}

%-----------------------------------------------------------------------

\newcommand{\eins}{\mathbb{1}}
\newcommand{\Acal}{\mathcal{A}}
\newcommand{\B}{\mathcal{B}}
\newcommand{\Ccal}{\mathcal{C}}
\newcommand{\Dcal}{\mathcal{D}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Ical}{\mathcal{I}}
\newcommand{\Lcal}{\mathcal{L}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Pcal}{\mathcal{P}}
\newcommand{\Prob}{\mathbb{P}}
\newcommand{\Qcal}{\mathcal{Q}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\Rdrei}{{\R^{3N}}}
\newcommand{\Tcal}{\mathcal{T}}
\newcommand{\T}{\mathbb{T}}
\newcommand{\Ucal}{\mathcal{U}}
\newcommand{\markov}{(X_t)_{t\in\T}}
\newcommand{\Xcal}{\mathcal{X}}
\newcommand{\X}{\mathbb{X}}
\newcommand{\inv}{^{-1}}
\newcommand{\afam}{\alpha_1,\dots,\alpha_n}
\newcommand{\bfam}{\beta_1,\dots,\beta_n}
\newcommand{\cfam}{\chi_1,\dots,\chi_n}
\newcommand{\lfam}{\lambda_1,\dots,\lambda_n}
\newcommand{\xfam}{\Xcal_1,\dots,\Xcal_n}
\newcommand{\pfam}{\psi_1,\dots,\psi_n}
\newcommand{\xtilde}{\widetilde{X}}
\newcommand{\xhat}{\widehat{X}}

\newcommand{\Sopt}{S_{\mathrm{opt}}}
\newcommand{\Sreal}{S_{\mathrm{real}}}

%\newcommand{\Sopt}{$S_\textrm{opt}$}
\newcommand{\detSopt}{\det(\Sopt)}
%\newcommand{\Sreal}{$S_\textrm{real}$}
\newcommand{\detSreal}{\det(\Sreal)}
\newcommand{\Spcca}{S_\mathrm{PCCA+}}
\newcommand{\detSpcca}{\det(\Spcca)}

\newcommand{\diag}{\mathrm{diag}}
\newcommand{\trace}{\mathrm{trace}}
\newcommand*\diff{\mathop{}\!\mathrm{d}}

\DeclareMathOperator*{\esssup}{ess\,sup}

%numerate only one lign in an align-environment
\newcommand\numberthis{\addtocounter{equation}{1}\tag{\theequation}}

\renewcommand{\labelenumi}{\roman{enumi})}

\newcommand{\todo}[1]{\textbf{\textcolor{red}{TODO: #1}}}

%widebar
\newcommand*\rel@kern[1]{\kern#1\dimexpr\macc@kerna}
\newcommand*\widebar[1]{%
  \begingroup
  \def\mathaccent##1##2{%
    \rel@kern{0.8}%
    \overline{\rel@kern{-0.8}\macc@nucleus\rel@kern{0.2}}%
    \rel@kern{-0.2}%
  }%
  \macc@depth\@ne
  \let\math@bgroup\@empty \let\math@egroup\macc@set@skewchar
  \mathsurround\z@ \frozen@everymath{\mathgroup\macc@group\relax}%
  \macc@set@skewchar\relax
  \let\mathaccentV\macc@nested@a
  \macc@nested@a\relax111{#1}%
  \endgroup
}